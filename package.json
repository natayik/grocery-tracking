{
  "name": "grocery-tracker",
  "private": true,
  "description": "Personal grocery price tracker — backend functions for push notifications and cloud sync.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "web-push": "^3.6.7"
  }
}
